# Automatically registered PHPStan entry point for an installed Composer package.
# Resolve from PHPStan's package root so Composer path-repository symlinks do not redirect the parent traversal.

includes:
    - %rootDir%/../../jbboehr/yumemi/yumemi-tags.neon
    - apocrypha.neon
